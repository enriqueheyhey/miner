<%= form_for(@transaction) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% @transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.text_field :user_id, class: "form-control", value: current_user.id, type: "hidden" %>
  <%= f.text_field :address_array, class: "form-control", value: '{{cartToString}}', type: "hidden" %>
  <br>
  <%= f.submit 'Confirm Purchase', class: "btn btn-primary", "ng-click" => "submitRailsForm()" %>
<% end %>
