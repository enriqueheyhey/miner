<p id="notice"><%= notice %></p><div ng-init="angularAddresses = <%= @addresses.to_json %>"></div>

<div class="container-fluid">
  <div class="header">
    <div class="left">
      <input class="form-control search" placeholder="Search Addresses ..." ng-model="searchQuery">
    </div>
    <div class="right text-right">
      <%= link_to 'New Address', new_address_path, class: "btn btn-primary" %>
    </div>
    <div class="clearfix"></div>
  </div>

<!-- <table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @addresses.each do |address| %>
      <tr>
        <td></td>
        <td>
          <%#= link_to 'Show', address %>
          <%#= link_to 'Edit', edit_address_path(address) %>
          <%#= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <%# end %>
  </tbody>
</table> -->

  <%= render 'table' %>
</div>